let blogs = JSON.parse(localStorage.getItem('blogs')) || [];

// loop over the blogs

// in the loop create the elements for the blog div. elements needed for username, title, content.
// append those elements to a div. append that div to the html(the element with the id of blogs)

// look at the activity for create and append
